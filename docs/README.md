# ratr0-engine documentation project

We use Sphinx, Breathe and Doxygen

Installation on Mac OS

Brew dependencies
```
brew install doxygen graphviz
```

Pip dependencies

```
pip3 install sphinx breathe sphinx-rtd-theme myst-parser
```